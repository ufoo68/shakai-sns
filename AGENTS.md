# Agent Instructions

## Development Workflow

- Before starting a new implementation, switch to `main` and update it with `git pull --ff-only`.
- Create a dedicated branch for the implementation before editing files.
- After implementation is complete, commit the changes, push the branch, and create a pull request.
